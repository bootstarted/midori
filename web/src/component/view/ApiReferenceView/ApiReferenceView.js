import * as React from 'react';

// import doctrine from 'doctrine';

import Header from '/component/partial/Header';

// const midori = require.context('../../../../../src');
// console.log(midori.keys());

class ApiReferenceView extends React.PureComponent {
  render() {
    return (
      <div>
        <Header />
        wee
      </div>
    );
  }
}

export default ApiReferenceView;
